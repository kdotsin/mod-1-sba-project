Here is my project reflection

To create the branches I did git checkout -b "branch name". To make sure I was in the correct branch I consistently used the syntax git branch just incase. So for the header I just created a simple html template in the index file while the footer branch had only the footer syntax within the index file. This caused a merge conflict where the footer syntax was on the top of the file and the html template was below it. All I needed to do was copy and paste the footer syntax within the body element of the html template. I didn't know exactly what you wanted me to do for pull request part but I created extra code within my project and made some purposeful typing errors so others can see it and comment on it. If classmate review it correctly, they'll mention my error and then I can make the change then merge. 
